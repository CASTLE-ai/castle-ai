# Local development environment.
#
# Pulls the core runtime stack from requirements.txt, then adds the test / lint /
# type-check / optional-feature tooling that CASTLE's QC gate relies on. On a
# Linux + NVIDIA workstation add the GPU accelerators as well:
#     pip install -r requirements-dev.txt -r requirements-gpu.txt
#
# CPU-only or CI install (equivalent, from setup.py):
#     pip install -e ".[dev,nwb,hdbscan]"
-r requirements.txt

# Test / lint / type
pytest>=7.0
ruff>=0.5
mypy>=1.10

# Optional-feature deps so the advertised commands are testable
pynwb>=2.5        # castle ethogram export-nwb
hdbscan>=0.8.33   # HDBSCAN clustering backend
